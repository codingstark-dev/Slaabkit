# Voice Cloudflare Example

Cloudflare Workers + Cloudflare Calls scaffold using `@Slaabkit/voice-ai`.

## Setup

1. Copy `.env.example` to `.dev.vars`.
2. Install dependencies from root.
3. Run `wrangler dev` in this directory.
4. Deploy with `wrangler deploy`.
