# Voice Daily Example

Browser + Daily WebRTC example using `@Slaabkit/voice-ai` transport stubs.

## Setup

1. Copy `.env.example` to `.env`.
2. Run local app and open browser page.
3. Provide Daily room URL/token and start session.

## Status

This example wires the app shell and transport integration points.
